function AboutHighlightCard({ title, text }) {
  return (
    <article className="about-highlight-card">
      <h3>{title}</h3>
      <p>{text}</p>
    </article>
  )
}

export default AboutHighlightCard
